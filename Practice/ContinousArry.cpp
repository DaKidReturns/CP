#include <cmath>
#include <cstdio>
#include <vector>
#include <iostream>
#include <algorithm>
using namespace std;


int main() {
    vector<int> arr;
    vector<int> sorted;
    int T,N,sa;
    int inc,dec;
    while(T-->0)
    {
		inc=0;
		dec=0;
		cin>>N;
		sa=0;
		for(int i=0;i<N;i++)
			{
				cin>>a;
				arr.push_back(a);
			}

		for(int i=0;i<arr.size()-1;i++)
		{
			if(arr[i]<arr[i-1])
			{
				
			}
		}
		
		
		}
    return 0;
}
